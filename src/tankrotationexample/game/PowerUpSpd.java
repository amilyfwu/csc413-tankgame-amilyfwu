package tankrotationexample.game;

import java.awt.*;
import java.awt.image.BufferedImage;

public class PowerUpSpd extends PowerUp{
    public PowerUpSpd(int x, int y, BufferedImage img, GameID id, Handler handler) {
        super(x, y, img, id, handler);
    }
}

package tankrotationexample.game;

import java.awt.*;
import java.awt.image.BufferedImage;

public class PowerUpHp extends PowerUp {
    public PowerUpHp(int x, int y, BufferedImage img, GameID id, Handler handler) {
        super(x, y, img, id, handler);
    }
}
